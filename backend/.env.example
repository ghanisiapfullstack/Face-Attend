# Salin ke backend/.env — jangan commit backend/.env

SECRET_KEY="dapegantenk"
DATABASE_URL=mysql+mysqlconnector://USER:PASSWORD@localhost:3306/face_attend
ALLOW_PUBLIC_REGISTER=true


#Catatan
# Akurasi wajah (semakin tinggi threshold = semakin ketat)
# FACE_MATCH_THRESHOLD=0.72
# FACE_MATCH_MARGIN=0.06
# Opsional: path embeddings (default ml_model/embeddings.json relatif proyek)
# EMBEDDINGS_FILE=